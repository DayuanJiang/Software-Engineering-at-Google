# 第十二章审校记录

## 范围与覆盖

- 已完整阅读973行双语章节，包括正文、列表、标题、25个编号示例、失败信息示例、脚注和提要，并完成第二轮逐段对照复核。
- 已读 `subagent-brief.md`、翻译技能及全部三份参考材料、共享术语表、第一章术语补充、README和获批修改样稿。按信息型、偏硬技术文体处理，润色自由度约3/10，准确性优先。
- 168个原始含中文行全部复核：修改131行，确认保留37行，挂起0行。未发现被解析为缩进代码块的中文；没有代码保护例外。
- 写入范围仅为指定章节和本目录。未编辑共享文件、调用子代理或外部模型 API，也未提交、推送或部署。未增译原先只有英文的内容。

## 术语与实质修正

- `check in / checked in` 译为“提交”，修正第44、52行，并恢复“当天剩下的时间”等限定。
- `test size` 为“测试规模”，指资源消耗及允许执行的操作；`test scope` 为“测试范围”，指验证多少代码。小型测试是规模分类，单元测试是范围分类，保留“通常如此，但并非总是如此”。
- `brittle test` 为“脆弱测试”，因无关且未引入缺陷的代码变更而失败；`flaky test` 为“不稳定测试”，没有代码变更也会非确定性地失败。`unchanging tests` 译为“无需改动的测试”，不再混同于结果稳定。
- `test double` 为“测试替身”；`test infrastructure` 为“测试基础设施”；`collaborators` 为“协作对象”；`setup` 在此译为“初始化”；打桩解释为预设测试替身的返回值。
- 交易处理示例中的 `transaction` 按账户和转账语境译为“交易”；未改变示例行为、参数、数值、名称或代码注释。
- 第58行补回工程师必须诊断并修复脆弱测试的漏译；第99行恢复“行为变更是唯一预期需要更新已有测试的情况”，并区分明确契约和无意形成的契约。
- 第283、291、295行澄清测试设定有误、作者离开后的维护困境及“可能整个存续期都无价值”的限定；没有把推测改成确定事实。
- 第379行补回“给定……，当……，则……”的行为描述，修正 `nontrivial methods` 的“非核心方法”误译；第400行把 `boilerplate` 改为“样板代码”，不是模板文件。
- 第495行恢复操作前的系统或环境状态；第547行恢复“一眼可见的正确性”，删除原译擅加的“这是不可能的”；第625行区分布尔值与被断言对象。
- DAMP强调描述清楚、含义明确，DRY强调减少重复，两者互补而非替代；保留一般测试代码与独立测试基础设施的不同要求。
- 第819行恢复否定方向：随机化默认值让工程师更难硬编码依赖，而非更加依赖；保留“有助于”的语气。第917行修正为通常不应要求工程师同时修改基础设施，而不是禁止他们修改。
- 第927行保留“可能增加维护成本却没有提高信心”的因果关系；第931行补回 `orders of magnitude` 的“几个数量级”。所有历史判断均按原书叙述，未作现代化改写。

## 保留的源文问题

- 第373行英文是 `first method`，随后却讨论 `second message`。第375行中文仍按本地英文译作“第一个方法”，未擅自推定为“第一条消息”；需由上级另行核对源文。
- 第326行中文图题中原有孤立的 `A.`。为保留所有英文字符，该片段未删除，只修正周围中文。第497行中文示例编号从12-1校正为英文对应的12-13。
- 第64行脚注末尾的 `/`、第774行的 `defaults7` 和原有脚注格式均保留。第164行原先粘连成 `12-3.2`，现分清示例12-3与普通文本“注2”；没有新增、删除或改动 Markdown 脚注标记。
- 第647行原文泛称希望测试在系统变化时失败；译文保留这一表述，没有擅自为英文补上“行为”限定，也没有据此改写前文关于无关变更的原则。
- 全部代码原样保留，包括例12-13的语言标签和 `negativeNumber = 10`、例12-14的拼写和围栏、例12-16的调用名称与括号、例12-18的 `result: =`，以及例12-22混排的语言和注释。这不是代码修复任务；教学示例中有意呈现的错误同样未修复。
- 上述事项是已审阅但按保护范围保留的英文、代码或格式问题，不是挂起的中文翻译。未联网补写源文，未检查外链可达性。

## 校验

- 对照固定 Git 快照 `110720f031b43e18708fdc38af03269b65b3cc41`，原始 SHA-256 为 `bb72e67677765117f77a553e0a44fdb4893d4e011a5ff434534a0cc05d2a6d80`，与共享基线及清单一致。
- `edits.json` 按原始1起始行号记录完整替换行；保留行显式列出，`held_lines` 为空。校验确认覆盖完整、互不重叠，清单可精确复现章节。
- 129个英文源行、142个英文片段、37个代码保护项（含27个围栏代码块）、11个解析链接、98个结构项、13个脚注标记不变。原章无HTML项，修改后仍无。
- 973行的行序、空行、缩进、行尾空白、硬换行均不变。英文标题和示例题注前缀不变，中文行内原有英文词及标识符的逐行计数不变。
- 额外核对完整 Markdown 块结构、非文本行内结构、源行映射，以及代码、链接和脚注的位置；原始行内代码、链接文本与目标均不变。
- 对全部168个中文行机械执行受保护的标点归一校验，结果已归一，残留0。没有修改或放宽共享审计工具的断言，也没有对其他章节运行不变性断言。
- 最终独立回放清单，与章节逐字节一致；重新计算的结果与落盘校验报告一致。7个只在内存中修改英文、注释、链接、HTML、硬换行或清单的反向检查均被拒绝。章节限定的空白检查通过。

```sh
uv run --offline --locked --project tools --cache-dir .cache/uv python -B translation-review/chapters/ch12/verify.py
git -c core.whitespace=-blank-at-eol diff --check -- zh-cn/Chapter-12_Unit_Testing/Chapter-12_Unit_Testing.md
```

`verify.py` 默认只读本章、原始快照、清单和基线；`--write-artifacts` 仅在校验通过后重新生成本目录的 `edits.json` 与 `verification.json`，不会修改章节。未运行示例代码或全站浏览器测试。
