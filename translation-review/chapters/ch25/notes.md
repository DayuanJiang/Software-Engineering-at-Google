# 第25章中文润色复核

## 范围与覆盖

- 按信息型、偏硬技术书籍处理，修改自由度约3/10：准确优先，其次是自然、清晰；不摘要、不扩写、不更新历史事实。
- 已完整阅读741行双语章节，包括标题、题记、列表、全部脚注、命令及其上下文；复核全部206个含中文的原始物理行。
- 修改185行，确认保留20行，另有1行暂缓；已完成205行，未检查行0。暂缓行不计作完成润色。
- 必读材料均已阅读：`translation-review/subagent-brief.md`、翻译润色技能及三份 reference、共享术语表、第一章术语补充和 README，并对照已批准的第一章样稿。
- 唯一暂缓项是第449行。第51行虽有四空格缩进，但它紧接第50行带硬换行的段落，解析器将其识别为普通段落续行，而非代码块，因此按中文引文润色。没有代码块例外，也没有需要暂缓的中文代码块。
- 只写入指定章节及本目录；未使用代理、外部模型 API、提交或推送，未修改共享工具、术语表或其他章节。

| 范围（原始行号） | 含中文行 | 修改 | 确认保留 | 暂缓 |
| --- | ---: | ---: | ---: | ---: |
| 导言，1至38 | 8 | 7 | 1 | 0 |
| 计算环境，39至194 | 46 | 38 | 8 | 0 |
| 托管计算软件，195至370 | 48 | 45 | 3 | 0 |
| 时间与规模，371至508 | 39 | 37 | 1 | 1 |
| 选择计算服务，509至716 | 56 | 51 | 5 | 0 |
| 总结与提要，717至741 | 9 | 7 | 2 | 0 |

## 术语取舍

| 原文 | 本章处理 |
| --- | --- |
| compute / compute resources / compute service | 按语境区分计算能力、计算资源、计算服务；不将机器、服务、作业视为同一对象。 |
| job / task / worker / replica | 作业／任务／工作进程（worker）／副本。worker 首次解释后保留英文；replica 不再误译为“复制”这一动作。 |
| batch job / serving job | 批处理作业／服务作业；前者运行至任务完成并关注吞吐量，后者持续处理请求并关注延迟。第449行的旧译仍属暂缓内容。 |
| scheduling / orchestration | 本地原文描述的是调度、实例生命周期和部署管理，按具体操作翻译；没有把这些概念一律扩大成“编排”，也没有把调度器等同于整个编排系统。 |
| rightsizing / autoscaling | 资源规格调优／自动伸缩；区分资源规格、资源用量、副本数量和扩缩容，不将 rightsizing 误译成权限调整。 |
| container / VM / persistent containers | 容器／虚拟机／常驻容器；常驻不表示本地状态持久化，容器与虚拟机也不是同一种隔离机制。 |
| transient state / persistent storage / immutable state | 临时状态／持久化存储／不可变状态；保留存活范围、持久性和可变性的区别。 |
| service discovery / indirection / idempotency | 服务发现／间接寻址／幂等性；不误作代理转发或“突变请求”。 |
| pets / cattle | 宠物模式／牛群模式；保留原文比喻，并明确实例可自动创建、替换的含义。 |
| common infrastructure / toil / implicit dependency | 共用基础设施／琐务／隐式依赖，沿用第一章及共享术语的边界。 |
| live migration / FaaS / power user | 热迁移／函数即服务／高级用户；power user 不等同于特权用户。 |
| trade-off | 权衡；分别保留资源成本与工程师时间、缓存冗余与故障风险、集中管理与定制、抽象与控制、锁定与管理开销的取舍。 |

## 主要修正

- 第93、101行：恢复机器分配、分时架构和迁移计算任务的含义，纠正“将计算机迁移到另一台机器”等误译。
- 第127、133、153行：恢复“影响较小”而非“不适用”、正常增长而非指数增长，以及内存耗尽时终止进程和磁盘交换导致延迟恶化的区别。
- 第171行：区分按资源用量统计的覆盖比例与按配置数量统计的覆盖比例。
- 第217、225、327行：将“删除新实例”改回创建新实例，恢复牛群模式的前提和主机名硬编码的限制。
- 第297、313行：恢复请求或数据块处理结束之后仍须存活的状态范围；checkpointing 是保存检查点，不是检查状态。
- 第335、343行：恢复修改状态的请求、客户端分配标识符，以及用退避、降级和抖动防止级联故障的关系。
- 第353至369行：保留 GB、TB、核心小时和整机 worker 的单位；恢复白板笔类比及工程师时间的机会成本。
- 第445、453、457行：纠正“服务于机器的人变成了牛”，区分预留容量、实际利用率和批处理使用闲置容量时的成本归属。
- 第483、498、503、507行：恢复记在脑中的 CLI 参数、预发布环境、任何时刻最多下线5%容量，以及性能和许可证这两个原先遗漏的动机。
- 第581、633、643、673、695行：修正无意传播高级功能、函数即服务、部分需求无法由无状态负载满足、共享集群成本分摊，以及抽象提高使扩容更容易的方向。

## 暂缓与源文问题

- **暂缓第449行，未改动。** 对应英文第447行明确写 `without significant latency decreases`，与上下文强调通过预留容量避免延迟恶化的论证冲突。若改译为“延迟上升”或“延迟恶化”，就会自行修正英文；若直接润色原译，又可能掩盖问题。因此整行逐字保留，登记在 `held_lines`，交由主审确认源文意图后处理。该行仍有旧术语和不顺表达，不声称已经完成。
- 第203至205行的原文计算示例同时给出100万份文档、每份1秒、200台机器和100分钟，理想均分的算术结果与100分钟并不严格一致；保留原文全部数字，不重算改写。第223至225行的50分钟例子也照原文保留。
- 第67／69行的 `Graphana`、第407／409行的 `PickUnu sedPortOrDie`、第519／521行命令中的 `./ my_binary`，均沿用本地既有写法，没有擅自修复源文或代码。
- 第425／427行先称“两阶段”，随后再谈第三阶段；保留这一叙述。`backport any relevant data` 语义较宽，译作“回补相关数据”，不擅自限定为某种数据迁移步骤。
- 第407／409行将 containers 与 Kubernetes 的 pods 对应，以及关于 PID 默认上限32,000的叙述，均按原书语境保留，不以当前技术文档改写。
- 第563至569、591至593、637至647、663至677行的云产品行为、无状态要求、缩容到零和谷歌架构选择，均为原著当时的论述，不声称它们是2026年的现状。2002、2003、2006、2007、2011、2012，以及“过去15年”“过去10年”“八年后”等时间信息均保留。
- 第663／665行的 `a part of a part of the workloads` 保留两层范围，不擅自将重复文字认定为排版错误。
- 第3行不完整的加粗、第24行残缺书签、第301行普通数字形式的脚注13、第393行路径周围原有星号解析方式，以及第470、487行没有目标的方括号文字均保留；本次不修复链接或排版结构。
- 没有查询外部原文，也未补写缺失英文；以上源文问题的处理不等于完成了官方版本校勘。没有新增译者注，更没有将原有说明混入作者正文。

## 验证

对照 `679725c08143c724598a2b89b94b8c8e4fa47688` 中的本章及 `translation-review/baseline.json`：

- 原始 SHA-256：`4e6540edaf161f91196d7b5df87cafca844c495b5ff77b75df642faca78b5eec`。
- 741行、全部非中文行、英文标题前缀、缩进、行结束符和硬换行保持不变；`edits.json` 可逐字节重建当前章节。
- 181个英文源行哈希、210个英文片段哈希、2个行内代码项、2条链接、46个脚注标记、109个结构项全部匹配；HTML 项为0。
- 完整 Markdown token 类型、标记和行范围匹配，未放宽共享校验器断言，也未添加代码哈希例外。
- 对185个修改行执行保护代码、链接、英文标题及英文书目信息的机械标点归一检查，残留0；不将第283行的英文书目引文改成中文标点。
- 74项资源、数量和历史文本断言通过；这些断言只是补充人工上下文复核，不等同于事实核查。
- 对修改行中的阿拉伯数字另作逐行比较，除千位分隔格式外，仅两处不同：第51行用“每台”承接50多台机器，避免重复数字；第669行删除原译“成功4地”中的无意义字符4。其他数值均保留。
- 10项负向校验通过：分别拒绝英文、代码、链接目标、标题前缀、硬换行、段落结构、资源单位、历史年份、暂缓内容和覆盖清单的变动。
- 校验结果为 `verified_with_holds`。全部原中文行已检查，但第449行未完成润色。

```bash
uv run --offline --locked --project tools --cache-dir .cache/uv python -B translation-review/chapters/ch25/verify.py
git -c core.whitespace=-blank-at-eol diff --check -- zh-cn/Chapter-25_Compute_as_a_Service/Chapter-25_Compute_as_a_Service.md translation-review/chapters/ch25/
```

`verify.py --write-artifacts` 仅在全部检查通过后，机械生成本目录的 `edits.json` 和 `verification.json`；默认模式只复核已有产物。未运行全仓断言、全站浏览器检查或外链可达性检查。
