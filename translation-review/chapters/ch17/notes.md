# 第17章中文润色记录

## 范围与风格

- 按 `translation-review/subagent-brief.md` 和已批准的 Chapter 1 样稿处理：信息型、偏硬技术文本，自由度约3/10；准确优先，不摘要、不更新历史事实。
- 已完整阅读第17章双语正文、标题、18条脚注、列表、查询示例，以及四张截图和其中的英文图注。
- 已阅读翻译技能 `SKILL.md` 及三个 `reference/` 文件、共享术语表、Chapter 1 的 README、术语补充和已批准正文样例。
- 仅修改本章 Markdown；新增文件均位于 `translation-review/chapters/ch17/`。未修改共享文件，未调用代理或外部模型 API，未提交。

## 精确覆盖

| 项目 | 原始物理行数 |
| --- | ---: |
| 全章 | 507 |
| 含中文的行 | 140 |
| 已修改 | 118 |
| 已复核并保留 | 22 |
| 暂缓中文行 | 0 |
| 中文标题 | 31 |
| 中文正文 | 82 |
| 中文脚注 | 18 |
| 中文列表项 | 9 |

140行中文全部完成复核。22行保留项逐一列在 `edits.json` 中；其中第291行中文提示语保留，损坏的地址另记为受保护的既有问题。上述“行”均指 Markdown 物理行，不是段落数或句子数。

## 术语与修正

- Code Search 沿用“代码搜索”，首次补明工具英文名；Critique、Piper、Kythe/Grok 等专名保留。中文脚注中的 `Kyth` 拼写按同段英文校正为 `Kythe`。
- ranking 用“排序”，query independent/dependent signals 用“查询无关／相关信号”，retrieval 用“检索”，reverse/inverted index 用“倒排索引”。
- token 在索引语境用“词项”，tokenization 用“分词”；代码浏览中的 token 用“词法单元”。substring 用“子串”，trigram 用“三元组”，保留已有 `trigram`、`ngram`、`n-gram` 技术拼写。
- 区分作用域与搜索范围、代码库与代码仓库、工作区与仓库、引用位置与用法种类；`head` 不译为“仓库头部”。
- 第14、18、38行：恢复内部 grep 类工具与外部 Code Search 的组合关系、整个机器集群的含义，以及交叉引用消歧的说明。
- 第50、54、80、94、100、120行：修正缺陷 ID 链接对象、版本差异比较、函数与功能、头文件与标题、错误类型漏译，以及 `blame` 被误译为“指责”。
- 第136、148、204、214、226、248行：修正增量索引、图的边与构建规则、GB/TB 量级、工程时间成本、按键与点击，以及 release 被误译为“释放”。
- 第268、272、280、325、329行：修正 CPU 核心与内核、估算未计入的开销、资源消耗“变为2.5倍”而非“增加2.5倍”、atomically 与 automatically 的混淆，以及 recency 被误译为“事件”。
- 第339、343、347、459、463行：按本地英文恢复被误译或遗漏的匹配样例 `appointed to the council`、`base`、`myproject`、`justmashedtogether` 和词干 `search`；修正限定名可以被搜索到的含义。原有查询操作符、大小写、空格和标识符未改写；`Alert` 的外侧误加空格按英文示例去除。
- 第367、383、387、489、507行：补回建议列表的多样性内容，理顺索引完整性与信任的条件关系，修正生成文件的索引说明、Noogler 入职培训和早期版本确实有人使用的含义。

## 截图与图注

已逐张查看以下图片，图注均嵌在图片内，没有新增或改写图注行：

- 图17-1：`The Code Search UI`，核对搜索建议、符号类型和 `symbol:BUILD`。
- 图17-2：`Code Search integration in a log viewer`，核对日志行和 `Open in Code Search` 菜单。
- 图17-3：`Code Search integration in stack frames`，核对栈帧到源代码、符号和版本限定的关联。
- 图17-4：`Code Search integration in documentation`，核对 `live-snippet`、`cs/` 查询及渲染的代码片段。横向滚动区右侧未完整显示，未猜测或补写隐藏的查询内容。

四张图片及 Markdown 图片路径均与原始 Git 快照、基线哈希一致；未更改任何 UI 标识符或截图代码。

## 保留的原文问题

- 第82行英文示例缺少闭引号。中文第84行只补齐本身的引号，不改英文。
- 第290至291行的参考地址原有异常：中文含缺失协议的地址和 `tp://`，英文第二个地址尾部含 `str_search./`。全部按原样保留，未猜测正确 URL，也未测试外链可达性。
- 第297、305、327、341、345、501行英文分别有 `query[^12] er`、`ontrast`／`apart rom`、`imilar`、`ion to`、`indable`、`t will` 等缺字或断裂。第341行尤有连接文字缺失；中文保留原译已有、上下文支持的“除文件内容外”关系，不补写缺失英文。
- 第337行英文例子的开闭引号不一致；英文原样保留，中文保留原例中的完整匹配文本，不将它改成中文释义。
- 第142行关于二次方成本的粗略说明涉及代码增长与 IDE 数量两个维度，中文保留原有论证与限定，不自行重建模型。
- 第465行原文用 `recall accuracy`，但后文讨论需要滤除不匹配结果。中文采用“召回结果的准确性”，不擅自把它改写成严格的“召回率”或新增漏报结论。
- 1.5 TB、每秒200次查询、50毫秒、10秒及每日构建交叉引用索引等数据，均作为原著历史叙述保留，没有声称核验其当前状态。
- 无代码块内中文或误缩进中文需要暂缓；`held_lines` 为 `{}`。以上原文与 URL 问题仍保留，不冒称已修复。

## 校验

基线提交：`110720f031b43e18708fdc38af03269b65b3cc41`。

原章 SHA-256：`0d28cb4b4fcfaec776ec417c6f69bdf6956354a7ae7c796204c782b52b23289a`。

`verify.py` 使用共享审计器的解析和保护函数，只验证本章，并额外检查逐行清单、查询字面量、Markdown 内联／块级结构及本章图片。未削弱或豁免任何基线断言。

- 118条替换与实际修改完全一致，118 + 22 = 140，复核分类不重叠、无遗漏。
- 507个物理行的顺序、空行、缩进、行尾空白和换行符不变；367个不含中文的行逐字不变。
- 121个英文源行哈希、143个英文片段哈希、4个行内代码项、10个链接项（含图片和裸 URL）、70个结构项、36个脚注标记不变。
- 本章原有围栏／缩进代码块0个、HTML项0个；未新增任何此类内容。
- 31项查询／标识符字面量断言通过；四张截图哈希不变。
- 机械标点检查通过：保护英文标题前缀、URL、代码及引号内英文查询后，中文规范化结果幂等，残留0项。
- `git -c core.whitespace=-blank-at-eol diff --check -- zh-cn/Chapter-17_Code_Search/Chapter-17_Code_Search.md` 通过。
- 12项仅在内存中进行的反向测试全部被校验器拒绝：改英文、改英文标题前缀、改行内 URL、改图片路径、改查询语法、改脚注标记、删行、改缩进、改硬换行、改 Markdown 列表结构、漏记覆盖行、修改清单与正文不符。

复跑命令：

```bash
env PYTHONDONTWRITEBYTECODE=1 UV_NO_SYNC=1 UV_NO_CACHE=1 uv run --offline --locked --project tools --cache-dir .cache/uv python translation-review/chapters/ch17/verify.py
```

环境变量用于避免写入共享缓存、同步依赖或生成字节码。`--record --write-report` 仅生成本目录的 `edits.json` 和 `verification.json`；通常复核无需这些参数。

未运行全书断言、在线原文核对或浏览器渲染。`verification.json` 记录的是本章的保护校验，不是外部链接或当前产品行为验证。
