# 第十章审校记录

## 范围与覆盖

- 按已批准的第一章风格处理：信息型、偏硬技术文本，自由度约 3/10，准确优先，不摘要化，不更新原著历史判断。
- 已通读完整双语章节，包括标题、列表、三个脚注、两组教程、全部代码与文档样例；已读审校说明、翻译技能及三份参考材料、共享术语表、第一章术语补充、README 和样稿。
- 全章656个物理行，174个原始含中文行全部检查。146行修改，26行确认保留，2行挂起；已完成中文172行，挂起行不计入完成数。
- 保留原文件名 `Chapter-10_Documentatio.md`，没有增删或移动行。只写本章及本目录，没有修改共享文件、启动代理、调用外部模型 API、提交或推送。

## 术语与实质修正

- `code review` 统一为“代码审查”；一般文档 `review` 使用“评审”或“审阅”，不机械套用代码术语。
- `owner / ownership` 按维护职责译为“负责人／责任归属”；`canonical documentation` 为“权威文档”，不是“经典”或单纯格式规范；`source control` 为“源代码版本控制”。
- `technical writer` 为“技术撰稿人”；`landing page` 在文档导航语境中为“入口页”；`seeker / stumbler` 为“查找者／浏览者”；`freshness` 为“时效性”，`freshness date` 根据后文定义译为“时效检查日期”。
- 第24行标题改为“什么算是文档”，恢复范围界定的含义。第83行恢复“原本不会被质疑”的否定。第148行纠正抱怨主体：是谷歌开发者抱怨文档，而非谷歌抱怨开发者。
- 第162、186、204、262行分别修正推广文档实践、帮助读者熟悉领域、面向专家可省略说明，以及另行生成参考文档的含义。
- 第314行将 `free functions` 改为“非成员函数”；第334行恢复“连贯的注释”，不再译为“松散的注释”。第401行明确每个完整用户操作对应一个步骤，保留用户动作与服务器自动动作的区别。
- 第548行恢复“完全掌握这部分是可选的”；第563行区分明确写作范围与泛泛的“编写内容”；第572、576行将“一件事”与“一句话”区分。
- 第600行恢复“避免让文档无人维护”，不误作“避免使用过时文档”。第630行恢复技术撰稿人侧重工程师日常职责之外任务的否定含义。第626、640行删除原译重复句。
- 保留概念文档、参考文档在清晰性、完整性和准确性之间的不同取舍，以及“可能”“通常”“约”等限定；保留2010年代末、1980年代末、7到10份、约90%、三个月等时间和数量。

## 挂起内容

| 原始行 | 原因与处理 |
| --- | --- |
| 397 | 教程中的 SQL 输入行原为 `7. 类型：创建数据库my_foobar_db；`。英文对应 `Type: CREATE DATABASE my_foobar_db;`。原译误翻了提示词及 SQL 关键字；因本次要求保留文档样例中的代码字面量，整行原样保留，待另行授权修复样例，不计为完成。 |
| 610 | 中文位于606至612行的围栏代码块中，是时效性元数据文档样例。按保护要求保留整块，包括中文注释、HTML 形状的分隔符、反引号、字段和日期，不计为完成。 |

未发现误解析为缩进代码块的普通中文段落。第610行是实际的围栏样例，不以“中文正文例外”为由放宽代码保护。

## 原有源文与样例问题

- 第616行中文段内的英文署名样例 `Last Review by…` 与第614行源文 `Last reviewed by...` 不一致。此次只润色周围中文，英文样例字面量原样保留；这不是已修复的源文问题。
- 教程中英代码块原有提示符差异：中文侧的 `$curl`、`$cp`、`$cd` 和 `baz:$CREATE` 与英文侧不同。部分展示 C++ 内容的围栏标为 `Java`。这些代码、语言标签和空白均未修改。
- 原有英文的 `g3doc.With`、`has established itself is` 等连写或语法问题，以及步骤说明与示例命令的关系，未在本次中文润色中改写。未联网比对官方全文，也未执行示例命令。
- 保留已有脚注标记与中英呈现差异，不新增标记，不补造英文，不把样例或补充说明并入作者正文。

## 校验

固定 Git 快照：`679725c08143c724598a2b89b94b8c8e4fa47688`。
原始 SHA-256：`efcca099cdcbd6886020479f7b1d211ad1c1c353a8f73f526296c1b406147f2e`，与共享基线一致。

- 保护校验通过，没有例外：151个英文源行、171个英文片段、13个代码块、8条解析链接、155个结构项、6个脚注标记均不变。独立 HTML 项为0；代码块中的 HTML 形状样例由代码哈希保护。
- 完整 Markdown 块及行内标记结构、属性、源行位置一致。行数、顺序、空行、缩进、行尾空白、换行符、英文标题前缀均保持不变。
- `edits.json` 以原始1起始行号记录完整替换行，逐行匹配当前章节。修改、确认保留、挂起三组互不重叠，合计覆盖174个含中文行。
- 中文标点已机械复核，172个已完成中文行的规范化残留为0。英文标签内部标点、链接、行内代码和挂起样例不作中文标点转换。
- 8项内存负向测试通过：英文正文、英文标题、围栏代码、链接目标、挂起 SQL、挂起元数据、列表编号、物理行数的改动均被拒绝；测试没有写入章节。
- 本章 `git diff --check` 通过。没有对其他章节运行不变性断言，没有修改或放宽共享校验工具。

复验命令：

```sh
uv run --offline --locked --project tools --cache-dir .cache/uv python -B translation-review/chapters/ch10/verify.py
git -c core.whitespace=-blank-at-eol diff --check -- zh-cn/Chapter-10_Documentation/Chapter-10_Documentatio.md
```

`verify.py` 默认只读。`--write-report` 仅更新本目录的 `verification.json`；`--write-manifest` 根据已人工审定的逐行改动及固定保留／挂起清单生成本目录的 `edits.json`，写入前同样执行全部保护校验。未运行全站浏览器测试或外链可达性检查。
