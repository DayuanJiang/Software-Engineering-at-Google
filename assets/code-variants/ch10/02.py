class AlphaNum:
    """The main parameter type for str_cat() and str_append().

    Provides efficient conversion of numeric, boolean, and hexadecimal values
    (through the Hex type) into strings.
    """
